# beam

beam is a command-line file transfer utility for local network.

## Installation

with Go:

```bash
go install github.com/mati-33/beam@latest
```

## Usage

send a file:

```bash
beam emit <file name>
```

receive a file:

```bash
beam absorb <beam code>
```
